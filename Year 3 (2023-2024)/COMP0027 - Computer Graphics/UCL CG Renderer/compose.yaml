version: '3'
services:
  uclcg:
    build: .
    volumes:
      - .:/usr/share/nginx/html
    networks:
      - internal-services
networks:
   internal-services:
     name: internal-services
     driver: bridge
